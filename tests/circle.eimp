var espace;
var retour;

function main {
1 params
10 locals
  $t0 <- 0;
  *{-8} <- $t0;
  $t0 <- *{-8};
  *{-44} <- $t0;
  $t0 <- 10;
  *{-12} <- $t0;
  $t0 <- *{-12};
  *retour <- $t0;
  $t0 <- 32;
  *{-16} <- $t0;
  $t0 <- *{-16};
  *espace <- $t0;
  while (
    $t0 <- $a1;
    *{-40} <- $t0;
    $t0 <- *{-40};
    $t0 <- $t0(+1);
    *{-36} <- $t0;
    $t0 <- *{-44};
    $t1 <- *{-36};
    $t0 <- $t0 < $t1;
    *{-32} <- $t0;
    $t0 <- *{-32};
    $t0
  ) {
    $s5 <- $a1;
    $s6 <- $a2;
    $s7 <- $a3;
    $t0 <- $a1;
    *{-20} <- $t0;
    $t1 <- *{-20};
    $a2 <- $t1;
    $t1 <- *{-44};
    $a1 <- $t1;
    call ligne;
    pop 0;
    $a1 <- $s5;
    $a2 <- $s6;
    $a3 <- $s7;
    $t0 <- *retour;
    *{-24} <- $t0;
    $t0 <- *{-24};
    putchar $t0;
    $t0 <- *{-44};
    $t0 <- $t0(+1);
    *{-28} <- $t0;
    $t0 <- *{-28};
    *{-44} <- $t0;
  }
}

function ligne {
2 params
18 locals
  $t0 <- 0;
  *{-8} <- $t0;
  $t0 <- *{-8};
  *{-76} <- $t0;
  while (
    $t0 <- $a2;
    *{-72} <- $t0;
    $t0 <- *{-72};
    $t0 <- $t0(+1);
    *{-68} <- $t0;
    $t0 <- *{-76};
    $t1 <- *{-68};
    $t0 <- $t0 < $t1;
    *{-64} <- $t0;
    $t0 <- *{-64};
    $t0
  ) {
    $t0 <- $a1;
    *{-52} <- $t0;
    $t0 <- $a1;
    *{-48} <- $t0;
    $t0 <- *{-52};
    $t1 <- *{-48};
    $t0 <- $t0 * $t1;
    *{-44} <- $t0;
    $t0 <- *{-76};
    $t1 <- *{-76};
    $t0 <- $t0 * $t1;
    *{-40} <- $t0;
    $t0 <- *{-44};
    $t1 <- *{-40};
    $t0 <- $t0 + $t1;
    *{-36} <- $t0;
    $t0 <- $a2;
    *{-32} <- $t0;
    $t0 <- $a2;
    *{-28} <- $t0;
    $t0 <- *{-32};
    $t1 <- *{-28};
    $t0 <- $t0 * $t1;
    *{-24} <- $t0;
    $t0 <- *{-36};
    $t1 <- *{-24};
    $t0 <- $t0 < $t1;
    *{-20} <- $t0;
    $t0 <- *{-20};
    if ($t0) {
      $t0 <- 46;
      *{-12} <- $t0;
      $t0 <- *{-12};
      putchar $t0;
    } else {
      $t0 <- 35;
      *{-16} <- $t0;
      $t0 <- *{-16};
      putchar $t0;
    }
    $t0 <- *espace;
    *{-56} <- $t0;
    $t0 <- *{-56};
    putchar $t0;
    $t0 <- *{-76};
    $t0 <- $t0(+1);
    *{-60} <- $t0;
    $t0 <- *{-60};
    *{-76} <- $t0;
  }
}

