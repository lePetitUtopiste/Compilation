function main {
1 params
8 locals
  $t0 <- 42;
  *{-8} <- $t0;
  $t0 <- *{-8};
  *{-36} <- $t0;
  while (
    $t0 <- 55;
    *{-32} <- $t0;
    $t0 <- *{-36};
    $t1 <- *{-32};
    $t0 <- $t0 < $t1;
    *{-28} <- $t0;
    $t0 <- *{-28};
    $t0
  ) {
    $t0 <- 50;
    *{-24} <- $t0;
    $t0 <- *{-36};
    $t1 <- *{-24};
    $t0 <- $t0 < $t1;
    *{-20} <- $t0;
    $t0 <- *{-20};
    if ($t0) {
      $t0 <- 2;
      *{-12} <- $t0;
      $t1 <- *{-12};
      $a2 <- $t1;
      $t1 <- *{-36};
      $a1 <- $t1;
      call add;
      pop -1;
      *{-36} <- $v0;
    } else {
      $t0 <- 1;
      *{-16} <- $t0;
      $t1 <- *{-16};
      $a2 <- $t1;
      $t1 <- *{-36};
      $a1 <- $t1;
      call add;
      pop -1;
      *{-36} <- $v0;
    }
    $t0 <- *{-36};
    putchar $t0;
  }
}

function add {
2 params
3 locals
  $t0 <- $a1;
  *{-16} <- $t0;
  $t0 <- $a2;
  *{-12} <- $t0;
  $t0 <- *{-16};
  $t1 <- *{-12};
  $t0 <- $t0 + $t1;
  *{-8} <- $t0;
  $t1 <- *{-8};
  $v0 <- $t1;
  return;
}

